Create a sample application of 4 pages with React Redux:
 Signup and Login page:  
Username and Password
With field level validation. Store the credentials locally with datetime information
Map view page (** Login required):
Display a map with some (2-3) different colored markers located anywhere across the globe as per your choice and when clicked on each marker it should open a popup which should display an image (of your choice) and description as to why you like that place. You can use any maps API such as google, bing, open street map etc.
Provide a colored filter which based on selection displays only the respective colored markers in the map
Login list (** Login required):
Display the list of users registered with datetime of registration along with login count
